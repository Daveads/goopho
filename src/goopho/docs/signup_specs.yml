example
---
tags:
  - Creating new user
parameters:
  - in: body
    name: body
    #required: true
    schema:
      $ref: '#/definitions/user'
responses:
  201:
    description: user created
    schema:
      id: user
      properties:
        name:
          type: string
          default: 'David Adejumo'
        password:
          type: string
          default: '12345'
        username:
          type: string
          default: 'daveads'
        email:
          type: string
          default: 'the.fucking@gmail.com'

